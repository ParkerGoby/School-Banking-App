# Database — used by both docker-compose and the backend
DATABASE_URL=postgresql://banking_user:changeme@postgres:5432/banking_db
POSTGRES_USER=banking_user
POSTGRES_PASSWORD=changeme
POSTGRES_DB=banking_db

# Security — generate SECRET_KEY with: openssl rand -hex 32
SECRET_KEY=4fe2504d5d0c8696edfe1ee98f84b73c1987a486581e832f6bdf70b90d0b7e8d
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# App
FRONTEND_ORIGIN=https://banking.school.local
ENVIRONMENT=production

# Caddy — set to the server's hostname or internal IP
CADDY_DOMAIN=banking.school.local
